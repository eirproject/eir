[package]
name = "libeir_passes"
version = "0.1.0"
authors = ["Hans Elias B. Josephsen <me@hansihe.com>"]
edition = "2018"
license = "MIT OR Apache-2.0"

[dependencies]
libeir_ir = { path = "../libeir_ir" }
libeir_intern = { path = "../libeir_intern" }
libeir_util_pattern_compiler = { path = "../util/libeir_util_pattern_compiler" }

matches = "0.1.8"

cranelift-entity = "0.30.0"

petgraph = "0.4"

bumpalo = { git = "https://github.com/hansihe/bumpalo.git", features = [ "collections_hash" ] }

fnv = "1.0.3"
